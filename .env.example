# ============================================================================
# AFE Frontend - Environment Configuration Template
# ============================================================================
#
# IMPORTANT: This is a TEMPLATE file. Copy this file to:
#   - .env                (for local development)
#   - .env.staging        (for staging environment)
#   - .env.production     (for production environment)
#
# SECURITY: Do NOT commit .env files to version control. Only .env.example
# should be tracked in git. All sensitive or environment-specific values
# must be configured in local .env files.
#
# ============================================================================
# API Configuration
# ============================================================================
#
# Backend API Base URL (with /api/v1)
# This URL must include the API version endpoint (e.g., /api/v1)
#
# Development:  http://localhost:8000/api/v1
# Staging:      https://api-staging.midominio.com/api/v1
# Production:   https://api.midominio.com/api/v1
#
VITE_API_URL=http://localhost:8000/api/v1

# Backend API Base URL (without /api/v1) - Used for direct file access (PDFs, XMLs)
# NUEVO 2025-11-18: Required for PDF viewing and downloads
#
# Development:  http://localhost:8000
# Staging:      https://api-staging.midominio.com
# Production:   https://api.midominio.com
#
VITE_API_BASE_URL=http://localhost:8000

# ============================================================================
# Request Configuration
# ============================================================================
#
# API request timeout in milliseconds
# Default: 30000 (30 seconds)
# Increase for slow networks or large uploads
#
# VITE_API_TIMEOUT=30000

# ============================================================================
# Application Environment
# ============================================================================
#
# Indicates the deployment environment
# Values: development, staging, production
# Used for logging levels and feature flags
#
# VITE_ENVIRONMENT=development

# ============================================================================
# Feature Flags (Optional)
# ============================================================================
#
# Enable/disable experimental features
# VITE_ENABLE_DEBUG_MODE=false
# VITE_ENABLE_ADVANCED_FILTERS=true

# ============================================================================
# End of Configuration
# ============================================================================
